# Changelog

## 2.1.2 - 2026-08-11

### Changed
- WebUI 侧边栏支持折叠收起（与 angel_memory 交互一致），收起后仅显示图标，展开/收起状态即时生效。
- 联系人监控支持群聊/私聊切换，名称不再限定群聊。
- `chat_status` 展示层在场超时推断：群聊安静超过在场超时后，WebUI 不再显示过期的「在场」。
- 输出重写新增句末句号清理；删除调试模式，`debug` 分组改名 `output_rewrite`。

## 2.1.0 - 2026-08-07

### 群聊独立配置（新功能）

之前所有群共用一套配置，想给不同群设置不同人设、不同回复风格，只能迁就全局。现在可以按群定制了：

- **配置模板**：可以创建多套模板（比如「游戏群」「学术群」），每套独立设置人设、唤醒词、回复长度、能量、回复节奏等
- **群聊绑定**：把模板应用到指定群聊，未绑定的群继续使用全局配置；绑定的群不再受全局改动影响
- **从全局复制**：新建模板时可直接复制当前全局配置作为起点，不用从零填

### 群聊监控（新功能）

插件页面新增「群聊监控」视图，实时查看每个群的运行状态：

- 当前是在场还是离场
- 剩余能量
- 巡检/等待进度
- 最近一次是否决定回复及原因

状态每 3 秒自动刷新，随时知道它在哪个群、什么状态。

### 其他改进

- 配置页面补充了每个字段的说明文字，对照官方设置项，不再靠猜
- 界面接入图标，导航和状态展示更直观

## 2.0.1 - 2026-08-06
- **修复群聊 `assistant_busy` 门闩死锁**（issue #64）：孤儿 running 工作导致秘书巡检每 30 秒无限重试、主动参与被冻结，只有重载插件能恢复。根因是 `start_work` 登记后唯一收口点 `after_message_sent` 在流式回复、主脑失败、空链等路径下不被 AstrBot 触发，`complete_work` 永不执行，running 工作永久残留。现已为工作账本增加 `running_timeout` 超时兜底（默认 300 秒，读取时惰性将超时未收口的 running 标记 failed，门闩自动解除），并在新工作登记时关闭同会话旧 running 工作，防止并发放行残留。
- 工作账本并发安全加固：`WorkLedger` 全部读写方法统一在可重入锁内执行，并支持注入时钟便于确定性测试。
- 新增测试钉死死锁行为：孤儿工作超时后巡检正常放行、零起点时钟与精确超时边界、跨会话隔离、新工作替换旧工作。

## 2.0.0 - 2026-07-28

这是 0.8.x 之后的一次**群聊调度与角色模型大重构**。如果你从 0.8.x 直接升到 2.0.0，最该关心的不是单个 bugfix，而是：

> 群聊“什么时候说话、谁在判断、为什么会累”已经换成了一套新机制。

### 为什么要升级

旧版（约 0.8.x）的群聊调度，核心依赖**单槽门锁 / 扣押队列**：

- 一条消息进来后，容易被“当前会话是否正在处理”卡死
- 被叫到、普通闲聊、回复后暂缓、不回复后暂缓，经常缠在同一套锁语义里
- 状态机里还残留 `SUMMONED` / `GETTING_FAMILIAR` 等旧路径，进场条件不清晰
- 连续插话、半句话抢答、@ 后被后续消息覆盖，都是这个旧模型的常见症状

2.0 的目标是把群聊调度改成更接近真实聊天伙伴的模型：

1. **角色拆开**：前台整理消息，秘书判断要不要接话，助理才真正说话
2. **状态收敛**：群聊只认离场 / 在场两态
3. **节奏拆开**：点名用助理防抖，普通闲聊用秘书巡检
4. **精力约束**：说得越多越累，长回复后不会立刻高频接话
5. **请求边界更干净**：上下文、图片、生命周期收口都按新调度重写

如果你觉得 0.8 里“被叫到却不回”“回完还连着插”“闲聊时太吵/太安静”不好控，2.0 就是为了解决这一层。

### 前后机制对照

#### 1. 角色模型

| 之前（0.8 文案/语义） | 现在（2.0） |
|---|---|
| 轻量级 AI / 分析员 | **秘书**：巡检并判断是否接话 |
| 重量级 AI / 专家 / 老板 | **助理**：主模型，实际生成回复 |
| 前端缓存 / 入口逻辑混称 | **前台**：整理消息、入账、调度入口 |
| 常混用“我 / AI / 唤醒” | 统一为前台 / 秘书 / 助理 / 点名 |

处理路径：

```text
收到消息 → 前台入账 → 秘书巡检 → 判断是否接话 → 助理生成回复
```

#### 2. 群聊状态机

| 之前 | 现在（2.0） |
|---|---|
| 离场 / 在场 + 旧兼容态混用 | 只认 **离场 `NOT_PRESENT`** / **在场 `OBSERVATION`** |
| 复读、密集聊天可能被理解成“混脸熟进场” | 复读 / 密集聊天**不会进场**；开启离场应答时只回复一次，回复后仍离场 |
| `SUMMONED` / `GETTING_FAMILIAR` 仍像业务状态 | 仅兼容旧数据/旧调用，**不再作为进场条件** |

#### 3. 调度核心：从单槽门锁到双防抖 + 巡检

| 之前 | 现在（2.0） |
|---|---|
| 旧单槽门锁 / processing 槽位 | **已退役**；调度只认双防抖 |
| 被叫到和普通消息常挤在同一等待逻辑 | **助理防抖**处理点名；**秘书巡检**处理在场普通消息 |
| 冷却未结束时行为不稳定 | 休息未结束时**保留最后边界事件并完整重计** |
| 同会话可能重叠多轮是否接话判断 | 同会话**秘书单飞**：只覆盖秘书判断阶段；交给助理后立即释放，最多一轮 |

新规则摘要：

- **点名**：有人 @ 或叫到助理 → 助理防抖（默认约 1 秒）→ 可升级/加速
- **普通巡检**：在场且群聊安静一段时间（默认约 30 秒）→ 秘书判断
- **边界替换**：等待中的新消息会 KILL 旧事件，只放行最后一条
- **秘书判断结束**：不回复时释放秘书单飞；回复时交给助理后立即释放，不再启动独立的不回复冷却
- **助理休息**：助理被调用后进入休息，普通巡检要等休息结束再考虑

#### 4. 能量 / 精力系统（2.0 新增）

0.8 没有“说多了会累”的运行时预算。2.0 为每个群增加独立精力：

- 普通巡检前按经过时间恢复
- 精力不够时**不进入秘书判断**，巡检重新计时
- 点名与离场一次性应答**不受精力门槛限制**；点名仍需通过助理忙碌和休息门闩
- 助理真正形成回复内容后扣能：

```text
消耗 = 基础消耗（默认 14） + 有效字符数 × 0.12
```

所以：

- 短回复更轻，恢复后更容易继续接话
- 长回复更累，之后普通闲聊会自然变少
- 被明确点名时会优先进入调度，不受精力门槛限制；仍需通过助理忙碌和休息门闩

精力按群保存在内存中，插件重启后回到初始值；配置在 `[能量设置]`。

#### 5. 上下文与请求重写

2.0 对上下文链路也做了大清理：

- 退役 `is_processed` 一类旧处理标记思路，改为更贴近工作账本 / 摘要块的消息管理
- 主脑重写钉在秘书判断点，避免“判断时看到的上下文”和“真正回复时看到的上下文”错位
- 整理与防抖解耦：整理期间不再乱触发防抖
- 注入助理工作账本系统提醒，让主模型看到自己的工作痕迹，而不是只看聊天原文
- 去除重复时间注入、群聊场景提示注入等噪声
- 完成点原子记录闭合 `assistant/tool` 链，减少半截工具上下文

#### 6. 图片

- 新增按需图片理解工具：主模型需要某张图细节时，再调用 AngelHeart 的图片工具
- 当前消息附件路径可替换为 AngelHeart 自己的 `media_cache` 路径，避免工具只看到 AstrBot 临时压缩图
- 图片转述失败时降级，不再把整条链路卡死

#### 7. 运行时清理

- 移除安慰计时器及其配置
- 退役旧单槽门锁、`processing_chats`、acquire/release 残留
- 插件停止时排空在途任务、完整清理运行态、停止已租借事件管线
- 群聊运行日志收敛，减少噪声

### 你可能感觉到的差异

从 0.8 升到 2.0 后，这些变化是**预期行为**，不是单纯“坏了”：

1. **闲聊接话不会那么密**  
   普通巡检要等安静，还要经过助理休息和精力判断。

2. **长回复之后会更安静一段时间**  
   因为长文本扣能更多；这是精力系统，不是随机漏回。

3. **被点名时会优先进入调度**
   点名走助理防抖，不受精力门槛限制，但到期放行仍需通过助理忙碌和休息门闩。

4. **复读 / 热聊不会把助理拉进持续在场**  
   最多离场应答一次；想持续参与，仍需明确点名。

5. **配置文案变了，部分旧键仅保留兼容读取**
   `no_reply_cooldown` 已不在当前配置界面中，也不再参与调度；当前有效的普通巡检抑制配置是 `waiting_time` 对应的助理休息。

6. **“老板画像”改成了“助理画像”**  
   只是展示口径；身份/策略配置仍用于判断哪些话题值得接。

7. **安慰词定时器没了**  
   不再靠后台安慰计时器维持存在感；节奏改由巡检、休息、精力控制。

### 配置与术语变化

界面统一为：

- **前台**：整理消息、调度入口
- **秘书**：巡检、判断是否接话
- **助理**：主模型实际回复
- **点名**：不再称唤醒 / 呼唤
- **巡检**：秘书等待并判断的过程
- **休息**：助理被调用后的普通巡检抑制；秘书单飞只覆盖判断阶段，不再主推“不回复冷却”口径
- **能量 / 精力**：助理会累，长发言更耗

主要新增/强化配置分组：

- `[回复节奏]`：点名等待、秘书巡检等待、连续点名加速、助理休息、在场保持
- `[能量设置]`：初始/上限/下限、恢复速度、回复消耗
- `[点名与交互]`：点名词、必须回应、静默控制
- `[助理画像]`：身份与关注范围

### 升级建议

1. 先确认秘书模型（轻量）和助理主模型都可用。
2. 若觉得太安静：先看能量是否过低、秘书巡检等待或助理休息是否过长。
3. 若觉得太吵：提高回复基础消耗或每字符消耗，或拉长助理休息时间。
4. 若只要被叫到才说话：开启“仅点名入场”，并确认点名昵称配置正确。
5. 从 0.8 直升时，重点回归这三条路径：
   - 点名必回
   - 在场普通闲聊的巡检接话
   - 长回复后的安静程度

### 2.0.0 发布前修订（截至 2026-07-28）

- 图片理解工具按 AstrBot 推荐结构拆入根目录 `tools/`，以 `FunctionTool` 和 `add_llm_tools()` 注册；工具名称、参数和图片账本权限边界保持不变。
- 普通巡检默认等待统一为 30 秒，助理休息也使用 `waiting_time` 控制。
- 秘书完成判断后立即释放单飞；回复交给助理后，后续生成和发送不再占用秘书判断位置。
- 群聊新增常规/焦点两档回复长度提醒：默认分别为 20 字和 200 字；命中焦点指令时使用较长提醒。
- 点名与焦点判断以消息正文入库结果为准，引用内容、昵称展示等非正文内容不会误触发；正文入库时会记录命中信息，供后续调度和回复长度判断复用。

### 本版本文档同步

- README 补三角色、巡检、能量说明，并保留 AngelMemory 组合介绍
- `_conf_schema.json` 展示文案统一为前台 / 秘书 / 助理 / 点名 / 休息
- `docs/angelheart_workflow.md` 对齐当前业务流程

## 0.8.40 - 2026-06-20
- 修复群聊上下文接管时 `req.prompt` 未被 AngelHeart 成功接管的问题：此前 `FrontDesk` 在秘书分析完成后过早将本轮消息 `mark_as_processed`，导致后续 `rewrite_prompt_for_llm()` 读取到的 `recent_dialogue` 为空，`final_prompt_str` 退化为空字符串，最终保留上游原始 `req.prompt`。现已移除回复路径中的提前标记，只在真正完成 prompt 重建后再标记；不回复路径继续立即标记，避免遗漏已处理状态。

## 0.8.39 - 2026-06-20
- **修复门锁冷却期间@呼唤被覆盖**（commit 99040f4）：`_is_summoned` 改为扫描「上次 AI 回复之后」的全部 user 消息，任意一条 `is_at_self=True` 即视为被呼唤。此前只看 ledger 中时间戳最新的一条 user 消息，门锁冷却期间多消息排队时，红豆的@消息可能被随后到达的非@消息覆盖判断，导致@后不回复。唤醒词检测仍只针对最新一条 user 消息，避免历史唤醒词反复触发。
- 新增呼唤判定路径的 debug 日志：cache_message 入库时记录 `is_at_self`、`event_id`、`timestamp`、`sender`；`_has_at_self_since_last_reply` 打印 `last_reply_ts`、扫描的 user 消息数量与逐条 `is_at_self`；`_is_summoned` 打印最终判定及命中分支。便于后续复现/定位呼唤未触发问题。

## 0.8.35 - 2026-06-08
- 修复私聊上下文渲染仍使用“群友”前缀的问题：私聊消息现在显示为 `[昵称 (ID: 账号)]`，群聊消息仍保持 `[群友: 昵称 (ID: 账号)]`。
- 修复上下文接管后当前轮图片被清空的问题：主模型支持图片时保留 AstrBot 原生 `req.image_urls`，不再因配置了 AngelHeart 图片转述而强制转述当前图片；主模型不支持图片时继续清理直传图片并按配置走转述。
- 对齐 AstrBot 的 provider 能力兼容策略：`modalities` 未声明或为空时不主动过滤图片，也不强制触发 AngelHeart 图片转述。
- 当前轮 prompt 会为多图和扣押聚合消息注入 `[图片1]`、`[图片2]` 等文本锚点，便于模型按顺序对应图片。
- 移除工具修饰消息功能：工具调用期间不再由 AngelHeart 额外发送拟人化提示，避免与模型自己的工具意图说明重复。

## 0.8.33 - 2026-05-27
- **新增渐进式上下文压缩算法**：替换原来的"只保留7条"策略，改为按 Token 预算从最新消息往前保留正文(10K)和工具消息(10K)，支持遗忘时间上限(默认1天)强制压缩。
- **配置 schema 重构为分组结构**：采用 `type: object` 嵌套分组（timing、leave_reply、wake_interaction、access_control、personality、context_compression、comfort、tool_decoration、debug），WebUI 展示更清晰。
- **新增配置自动迁移**：升级时自动将旧的扁平配置迁移到新的分组结构，用户配置值不丢失。
- **修复离场应答开关失效**：`leave_echo_reply` 和 `leave_dense_reply` 之前未作为开关使用，关闭后复读/密集检测仍会触发。现已修复。
- **修复混脸熟超时未生效**：`familiarity_timeout` 参数之前从未被任何代码读取，混脸熟状态永远不会超时降级。现已接入超时检查逻辑。
- **修复上下文接管不尊重白名单**：`delegate_prompt_rewriting` 现在会检查白名单，启用白名单时只接管白名单内的会话。

## 0.8.32 - 2026-05-27
- 私聊重写 LLM 请求体时也会在上下文不足时从 AstrBot 官方 conversation history 回退补充历史，避免只看到当前一条消息。
- 非群聊补历史现在直接使用 AstrBot 官方 conversation history，不再尝试 QQ 群历史接口。
- AstrBot 官方 history 回退最多取 7 条有效正文消息，正文累计最多约 10K token，并跳过工具调用与工具结果，避免将过大的已生成上下文灌入 AngelHeart 总账。
- 新增 AstrBot history 回退转换测试，覆盖 7 条限制、工具消息跳过、多模态文本块提取与 10K 正文限制。

## 0.8.31 - 2026-05-27
- 调整默认配置：安抚机制默认每 60 秒发送一次，安心词默认改为“要给”；张嘴词默认改为“张嘴”；观测中超时时间默认改为 60 秒。
- 删除未生效的“离场应答超时时间”配置项，并在配置迁移时自动清理旧配置中残留的 `familiarity_timeout`。
- 上下文压缩的 Token 上限现在会读取当前会话绑定模型的 `max_context_tokens`，并与插件侧上限取较小正数，便于直连模型使用大上下文、群聊使用较小上下文。
- 不再因离场/不在场状态强制清理上下文；上下文删减只由压缩策略和消息数量上限控制。
- 补历史时不再只保留最新一条当前消息，改为合并历史消息与当前内存消息，避免补历史阶段误清空已有上下文。
- 提示词中的消息时间默认改为系统当地绝对时间，不再默认显示“刚刚”“X 分钟前”等相对时间。

## 0.8.29 - 2026-04-30
- 新增独立配置项 `takeover_private_chat_context`，用于单独控制是否接管私聊上下文。
- 群聊与私聊的上下文接管判断现已彻底拆分：群聊只看 `group_chat_enhancement`，私聊只看 `takeover_private_chat_context`，两者互不依赖。
- 统一群聊/私聊上下文接管配置文案，明确默认行为为“默认接管群聊，不接管私聊”。

## 0.8.28 - 2026-04-30
- 提高秘书 JSON 解析鲁棒性：`should_reply` 现在是唯一关键字段，其余字段类型错误或缺失时会自动回退默认值，不再因辅助字段异常导致整条决策失败。
- 对 `entities`、`facts`、`keywords` 新增字符串数组归一化处理，兼容单字符串、空值、混杂脏数据等异常输出。
- 新增可直接运行的手动测试脚本 `tests/manual_test_llm_analyzer_json_robustness.py`，覆盖多组刁钻和弱智输出案例，用于脱离上游环境验证秘书解析稳定性。

## 0.8.27 - 2026-04-30
- 新增配置项 `block_unapproved_wake_non_command`，可阻断未通过 AngelHeart 批准的非命令唤醒消息，禁用这类消息对上游与其他插件的后续唤醒效果。
- 上游 command/skill 事件现在会在入口层直接跳过 AngelHeart，不进入聊天记录。
- 普通唤醒非命令消息不再在 `_should_process()` 中被误判为“指令或@他人消息”提前忽略，改为进入后续规则处理。
- 未命中上游额外聊天唤醒前缀的非命令消息现在会保留聊天记录，但跳过秘书分析；启用阻断开关时会进一步停止后续主 LLM 处理。

## 0.8.26 - 2026-04-21
- 新增 AngelHeart 专用图片转述模型配置 `image_caption_provider_id`，与上游图转文配置解耦，避免重复转述。
- 图片转述结果现在会按统一的 `<图片附件>` 文本块格式注入到消息中，包含图片路径与图片描述。
- 组建请求前会先补齐待回答消息的图片转述，最新消息正文也不再混入 `[图片]` 占位。

## 0.8.25 - 2026-04-18
- 新增“张嘴词”配置 `speak_words`，命中后可立即解除当前会话的闭嘴状态。
- 重建 LLM 请求体时，若重建出的当前提示词为空，则仅重建上文并保留原始 `req.prompt`，避免将当前轮输入覆盖为空。

## 0.8.24 - 2026-04-17
- 白名单模式下，安抚词机制现在只对白名单内会话生效；非白名单会话不会启动或继续发送安抚消息。

## 0.8.23 - 2026-04-16
- 消息过滤增强：对回复文本进行 markdown 清洗后，如果为单行且末尾是“.”或“。”，自动去掉最后一个句号，避免单行结尾多余标点。

## 0.8.22 - 2026-04-16
- 重构上下文注入：未处理消息作为最新 `prompt` 注入主模型，秘书建议改为临时上下文并标记为不持久化。

## 0.8.21 - 2026-03-21
- 重建上下文时，用户消息改为追加独立时间文本块，时间值使用该消息的发送时间（`YYYY-MM-DD HH:MM (CST)`）。
- 不再注入 `Current datetime`，避免将“当前时间”误当作消息时间造成时序干扰。

## 0.8.20 - 2026-03-20
- 私聊消息现在也会进入 AngelHeart 的上下文重写链路，发送给模型前会按总账历史重建聊天记录。
- 私聊保持主框架直接响应，不再误走群聊秘书判定和群聊补历史逻辑。
- 修复私聊唤醒消息可能在前置过滤阶段被跳过，导致上下文未入账的问题。

## 0.8.19 - 2026-03-16
- 发布维护版本：同步主分支当前修复并更新发布号。
- 无额外行为变更，功能与 `0.8.18` 保持一致。

## 0.8.18 - 2026-03-13
- 新增图片原始引用字段 `original_file_url`，统一保留 file/url 来源信息。
- 重建上下文时为含图消息追加 `Image Ref` 文本块，支持历史图片引用被 LLM 持续感知。
- 保持原有多模态内容结构不变，兼容现有图片处理链路。

## 0.8.17 - 2026-03-12
- 修复混脸熟状态下触发类型固定为密集讨论的问题，现可正确识别复读触发并使用对应策略。

## 0.8.15 - 2026-03-10
- 修复“必须响应呼唤”开启时，消息命中昵称却未进入“被呼唤”状态的问题。

## 0.8.14 - 2026-03-09
- 新增“必须响应呼唤”配置项，可控制被呼唤时是否强制回复。
- 默认保持开启，兼容现有使用习惯。
